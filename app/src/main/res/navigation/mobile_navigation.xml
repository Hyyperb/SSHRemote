<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_panel">

    <fragment
        android:id="@+id/nav_panel"
        android:name="com.hyyperb.sshremote.ui.panel.Panel"
        android:label="@string/menu_panel"
        tools:layout="@layout/fragment_panel" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="com.hyyperb.sshremote.ui.settings.SettingsFragment"
        android:label="@string/menu_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>